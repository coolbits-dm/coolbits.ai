# Milestone M3 — Chat Engine + OpenAI Integration

## Objective
Ship the Express gateway and chat handler that uses OpenAI, dynamic model selection, rate limiting, CORS, and ensures disclaimers appear in every response.

## Deliverables
- `app/server/chat.js` with `handleChat`, multi-intent handling, smalltalk safeguards, and OpenAI client logic.
- `app/server/server.js` wiring Express to `handleChat`, enabling `/api/chat` and `/health` endpoints.

## Technical Notes
- Selected models: gpt-4.1-mini for short questions, gpt-4.1 for standard, gpt-5.1 for DevOps intensity.
- Config loaded from `server/config/config.json`, including the new disclaimer field about Andy.
- Prepared for PM2/Ecosystem deployment and eventual integration with Camarad’s logging pipeline.
