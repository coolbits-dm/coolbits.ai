name: guardrails

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
      - master
      - cleanup/prune-non-cbpl

concurrency:
  group: guardrails-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  guardrails:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
      
      - name: Connectors registry guardrails
        run: node tools/connectors-registry-test.js

      - name: CBPL guardrails
        run: node tools/cbpl-guardrails-test.js
